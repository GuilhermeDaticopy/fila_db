{
  "name": "sistema-fila-completo-heroku",
  "version": "1.0.0",
  "description": "Deploy do frontend e backend juntos no Render",
  "scripts": {
    // O script "postinstall" foi REMOVIDO daqui.
    // A lógica de build do frontend será movida para o "Build Command" no Render.
  },
  "engines": {
    "node": "20.x"
  }
}
